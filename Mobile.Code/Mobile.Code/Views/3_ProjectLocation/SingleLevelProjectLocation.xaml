<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Mobile.Code.Views._3_ProjectLocation.SingleLevelProjectLocation"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:Mobile.Code.Controls"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:ffimageloading="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
    xmlns:helpers="clr-namespace:Mobile.Code.Helpers"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:uc="clr-namespace:Mobile.Code.Controls"
    xmlns:yummy="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView"
    Shell.NavBarIsVisible="True"
    mc:Ignorable="d">
    <ContentPage.Padding>
        <OnPlatform x:TypeArguments="Thickness">
            <On Platform="iOS" Value="0,45,0,0" />
        </OnPlatform>
    </ContentPage.Padding>
    <Shell.TitleView>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Label
                FontAttributes="None"
                FontFamily="{StaticResource Montserrat-Regular}"
                FontSize="Subtitle" 
                HorizontalOptions="CenterAndExpand"
                Text="{Binding Project.Name}"
                TextColor="White"
                VerticalOptions="CenterAndExpand" />

        </Grid>
    </Shell.TitleView>
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Invasive"
                 IconImageSource="{FontImage FontFamily={StaticResource MaterialFontFamily},
                 Glyph={x:Static helpers:IconFont.ShieldRefresh},
                 Color=#FFD180,Size=45}" IsEnabled="{Binding CanInvasiveCreate}"
                 Order="Primary" Command="{Binding CreateInvasiveCommand}"
                 Priority="1" />
        <ToolbarItem Text="Download"
                 IconImageSource="{FontImage FontFamily={StaticResource MaterialFontFamily},
                 Glyph={x:Static helpers:IconFont.CloudDownload},
                 Color=#FFD180,Size=45}" IsEnabled="{Binding IsInvasive}" 
                 Order="Primary" Command="{Binding DownloadOfflineCommand}"
                 Priority="1" />

        <ToolbarItem Text="Edit"
                 IconImageSource="{FontImage FontFamily={StaticResource MaterialFontFamily},
                 Glyph={x:Static helpers:IconFont.FileDocumentEdit},
                 Color=#FFD180,Size=45}" IsEnabled="{Binding IsEditDeleteAccess}"
                 Order="Secondary" Command="{Binding EditCommand}"
                 Priority="1" />
        <ToolbarItem Text="Delete"                     
                     IconImageSource="{FontImage FontFamily={StaticResource MaterialFontFamily},
                     Glyph={x:Static helpers:IconFont.Delete},
                     Color=Red,Size=45}" IsEnabled="{Binding IsEditDeleteAccess}"
                     Command="{Binding DeleteCommand}" IsDestructive="True"
                     Order="Secondary"
                     Priority="2" />
        
        
        <ToolbarItem Text="Home"
                 IconImageSource="{FontImage FontFamily={StaticResource MaterialFontFamily},
                 Glyph={x:Static helpers:IconFont.Home},
                 Color=#FFD180,Size=45}"
                 Order="Secondary" Command="{Binding GoHomeCommand}"
                 Priority="1" />
        
    </ContentPage.ToolbarItems>
    <AbsoluteLayout>
        <controls:Loading
            AbsoluteLayout.LayoutBounds="0,0,1,1"
            AbsoluteLayout.LayoutFlags="All"
            IsVisible="{Binding IsBusyProgress}" />
        <Grid BackgroundColor="{DynamicResource Orange-300}" 
              AbsoluteLayout.LayoutBounds="0,0,1,1"
            AbsoluteLayout.LayoutFlags="All">
            <Grid  VerticalOptions="Fill"           
                IsVisible="{Binding IsBusyProgress, Converter={StaticResource InverseBoolConverter}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Picker
                    x:Name="offlineProjectPicker"
                    Margin="0,10,0,0" TextColor="White"
                    BackgroundColor="{DynamicResource Orange-300}"                   
                    HorizontalOptions="CenterAndExpand"
                    IsVisible="{Binding IsOnline}"
                    ItemDisplayBinding="{Binding Name}"
                    ItemsSource="{Binding OfflineProjects}"
                    Title="Select Project to Sync with"
                    SelectedIndexChanged="offlineProjectPicker_SelectedIndexChanged"
                    SelectedItem="{Binding SelectedOfflineProject}"
                    VerticalOptions="StartAndExpand"
                     />
                <Grid x:Name="gridTopPart" Grid.Row="1">
                    <yummy:PancakeView CornerRadius="10" Margin="8,4" >
                        <ffimageloading:CachedImage 
                        Aspect="AspectFill" Opacity="0.8"
                        DownsampleToViewSize="True"
                        ErrorPlaceholder="blank.png"
                        HeightRequest="220"
                        LoadingPlaceholder="loading.png"
                        Source="{Binding Project.ImageUrl}" />
                    </yummy:PancakeView>

                    <Label
                    Margin="15,10"
                    FontFamily="{StaticResource Montserrat-Bold}"
                    FontSize="Caption"
                    HorizontalOptions="Start"                    
                    Text="{Binding Project.CreatedOn}"
                    VerticalOptions="End"
                    VerticalTextAlignment="Center" />

                    <Label x:Name="lblInvasive" Padding="2"
                        Margin="15,10" BackgroundColor="{DynamicResource Orange-100}"
                        FontFamily="{StaticResource Montserrat-Bold}"
                        FontSize="Body"
                        HorizontalOptions="EndAndExpand"                            
                        Text="Invasive Report"
                        TextColor="{DynamicResource Orange-900}"
                        VerticalOptions="StartAndExpand"
                        VerticalTextAlignment="Center" />
                </Grid>

                <Grid Grid.Row="2" RowDefinitions="auto,*" x:Name="FlexTypesVisualForm">
                    <StackLayout Grid.Row="0" Margin="5,0,0,0">
                        <Label
                                Margin="5,0,0,0"
                                FontFamily="{StaticResource Montserrat-Regular}"
                                FontSize="Caption"
                                LineHeight="1.5"
                                Text="{Binding Project.Description}"
                                TextColor="{DynamicResource  ColorForegroundText}" />
                    </StackLayout>
                    <ScrollView Grid.Row="1" >
                        <CollectionView  Margin="10,5" BackgroundColor="{DynamicResource Orange-300}" VerticalOptions="Center"
                                    EmptyView="No locations to display, use &quot;+&quot; button to add new locations."
                        
                        ItemsSource="{Binding VisualFormProjectLocationItems}"
                        IsVisible="{Binding IsViusalReport}">
                            <CollectionView.ItemsLayout>
                                <LinearItemsLayout Orientation="Vertical"
                                            ItemSpacing="8" />
                            </CollectionView.ItemsLayout>
                            <CollectionView.ItemTemplate >
                                <DataTemplate>
                                    <Frame CornerRadius="8"
                                                IsClippedToBounds="True"
                                                Margin="4" 
                                                Padding="0" 
                                                BorderColor="{DynamicResource Orange-100}"
                                                HasShadow="True">
                                        <!--<Frame.Triggers>
                                            <DataTrigger TargetType="Frame"
                                                         Binding="{Binding FurtherInasive}" Value="True">
                                                <Setter Property="TranslationX" Value="5" />
                                                
                                            </DataTrigger>
                                        </Frame.Triggers>-->
                                       
                                        <Frame.Background>

                                            <LinearGradientBrush StartPoint="0,0"
                               EndPoint="1,1">
                                                <GradientStop Color="{DynamicResource Orange-100}"
                            Offset="0.1" />
                                                <GradientStop Color="{DynamicResource Orange-400}"
                            Offset="0.6" />
                                                <GradientStop Color="{DynamicResource Orange-700}"
                            Offset="1.0" />
                                            </LinearGradientBrush>
                                        </Frame.Background>
                                        <SwipeView >

                                            <Grid RowDefinitions="Auto,Auto,auto" 
                                              ColumnDefinitions="auto,auto,*"
                                                x:Name="FrameTypes" 
                                                ColumnSpacing="0"
                                                RowSpacing="0" IsClippedToBounds="True">
                                                <Grid.Background>

                                                    <LinearGradientBrush StartPoint="0,0"
                               EndPoint="1,1">
                                                        <GradientStop Color="{DynamicResource Orange-100}"
                            Offset="0.1" />
                                                        <GradientStop Color="{DynamicResource Orange-400}"
                            Offset="0.6" />
                                                        <GradientStop Color="{DynamicResource Orange-700}"
                            Offset="1.0" />
                                                    </LinearGradientBrush>
                                                </Grid.Background>

                                                <Label Margin="8,2"
                                                Grid.Column="0" Grid.ColumnSpan="2"
                                                FontFamily="{StaticResource Montserrat-Regular}"
                                                FontSize="Body"
                                                HorizontalOptions="StartAndExpand"
                                                HorizontalTextAlignment="End"
                                                Text="{Binding Name}"
                                                TextColor="{DynamicResource ColorForegroundText}" />
                                                <Frame Grid.Column="2" HorizontalOptions="End" Margin="2,2,10,2" IsVisible="{Binding IsPostInvasiveRepairsRequired}" 
                                                       CornerRadius="15" Padding="0" HeightRequest="30" HasShadow="True">
                                                    <Image  HorizontalOptions="End"  
                                                       IsAnimationPlaying="True" 
                                                       Source="conclusive_new.gif" HeightRequest="30"/>
                                                </Frame>

                                                <Label Padding="0"
                                                Grid.Row="1" Margin="8,5,0,0"
                                                Grid.Column="0"
                                                VerticalOptions="CenterAndExpand"
                                                FontFamily="{StaticResource Montserrat-Regular}"
                                                FontSize="Small"
                                                HorizontalOptions="StartAndExpand"
                                                HorizontalTextAlignment="Center"   
                                                
                                                TextColor="{DynamicResource ColorForegroundText}" >
                                                    <Label.FormattedText>
                                                        <FormattedString>
                                                            <Span Text="Visual Review: " />
                                                            <Span Text="{Binding VisualReview}" FontAttributes="Bold"/>
                                                        </FormattedString>
                                                    </Label.FormattedText>

                                                </Label>
                                                <Label Margin="5,5,10,0"
                                                Grid.Row="1"
                                                Grid.Column="2"
                                                
                                                FontFamily="{StaticResource Montserrat-Regular}"
                                                FontSize="Small"
                                                HorizontalOptions="End"
                                                HorizontalTextAlignment="Start"
                                                
                                                TextColor="{DynamicResource ColorForegroundText}">
                                                    <Label.FormattedText>
                                                        <FormattedString>
                                                            <Span Text="Further Invasive Required: "/>
                                                            <Span Text="{Binding FurtherInasive}" FontAttributes="Bold"/>
                                                        </FormattedString>
                                                    </Label.FormattedText>
                                                </Label>

                                                <Label Grid.Row="2"
                                                Grid.Column="0"   Margin="8,5,0,0"                                                                                            
                                                HorizontalOptions="StartAndExpand" 
                                                       FontFamily="{StaticResource Montserrat-Regular}"
                                                FontSize="Small"
                                                VerticalOptions="CenterAndExpand"
                                                HorizontalTextAlignment="Start"
                                                
                                                Text="{Binding FurtherInasive}"
                                                TextColor="{DynamicResource  ColorForegroundText}"
                                                >
                                                    <Label.FormattedText>
                                                        <FormattedString>
                                                            <Span Text="Condition Assesment: "/>
                                                            <Span Text="{Binding ConditionAssessment}" FontAttributes="Bold"/>
                                                        </FormattedString>
                                                    </Label.FormattedText>
                                                </Label>
                                                <Label Grid.Row="2" Margin="5,5,10,0"
                                                Grid.Column="2"                                                                                               
                                                HorizontalOptions="End" 
                                                       FontFamily="{StaticResource Montserrat-Regular}"
                                                FontSize="Small"
                                                VerticalOptions="CenterAndExpand"
                                                HorizontalTextAlignment="Start"
                                                
                                                Text="{Binding FurtherInasive}"
                                                TextColor="{DynamicResource  ColorForegroundText}"
                                                >
                                                    <Label.FormattedText>
                                                        <FormattedString>
                                                            <Span Text="Visual Signs of Leak : "/>
                                                            <Span Text="{Binding AnyVisualSign}" FontAttributes="Bold"/>
                                                        </FormattedString>
                                                    </Label.FormattedText>
                                                </Label>
                                            </Grid>

                                            <SwipeView.RightItems>
                                                <SwipeItems>
                                                    <SwipeItem Text="Delete" IsDestructive="True"
                                                           IconImageSource="{FontImage FontFamily={StaticResource MaterialFontFamily},
                                                               Glyph={x:Static helpers:IconFont.Delete},
                                                               Color=#e65100,
                                                               Size=26}"
                                                           BackgroundColor="{DynamicResource Orange-900}"
                                                           Command="{Binding BindingContext.DeleteVisualFormCommand,Source={x:Reference FlexTypesVisualForm}
                                                }" CommandParameter="{Binding .}" />
                                                </SwipeItems>
                                            </SwipeView.RightItems>
                                            <SwipeView.LeftItems Mode="Execute">
                                                <SwipeItems>
                                                    <SwipeItem Text="Edit"
                                                           IconImageSource="{FontImage FontFamily={StaticResource MaterialFontFamily},
                                                               Glyph={x:Static helpers:IconFont.BriefcaseEdit},
                                                               Color=Blue,
                                                               Size=26}"
                                                           BackgroundColor="{DynamicResource Orange-400}"
                                                           Command="{Binding BindingContext.GoToVisualFormCommand,Source={x:Reference FlexTypesVisualForm}
                                                }" CommandParameter="{Binding .}" />

                                                </SwipeItems>
                                            </SwipeView.LeftItems>

                                            <SwipeView.GestureRecognizers>
                                                <TapGestureRecognizer Command="{Binding BindingContext.GoToVisualFormCommand,Source={x:Reference FlexTypesVisualForm}}"
                                                              CommandParameter="{Binding . }" />
                                            </SwipeView.GestureRecognizers>
                                        </SwipeView>
                                        <!--<Frame.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding BindingContext.GoToVisualFormCommand,Source={x:Reference FlexTypesVisualForm}}"
                                                              CommandParameter="{Binding . }" />
                                        </Frame.GestureRecognizers>-->
                                    </Frame>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </ScrollView>

                </Grid>

            </Grid>
            <Frame Margin="10,10,20,30" CornerRadius="40" Padding="0" 
               VerticalOptions="EndAndExpand" Opacity=".9"
                   HorizontalOptions="End" IsClippedToBounds="True"
                   HasShadow="True" IsVisible="{Binding IsInvasiveControlDisable, Converter={StaticResource InverseBoolConverter}}"
                   BorderColor="{DynamicResource  ColorBackground}"
                   HeightRequest="70" WidthRequest="70"
                   BackgroundColor="{DynamicResource Orange-900}">

                <Label  Padding="0"                   
                        FontSize="45"
                        HorizontalOptions="CenterAndExpand"
                        Text="+" HorizontalTextAlignment="Center"
                        TextColor="{ DynamicResource ColorBackground}"
                        VerticalOptions="CenterAndExpand" />
                <Frame.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding NewViusalReportCommand}" />
                </Frame.GestureRecognizers>
            </Frame>
        </Grid>


    </AbsoluteLayout>

</ContentPage>