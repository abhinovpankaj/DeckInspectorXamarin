<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:vm="clr-namespace:Mobile.Code.ViewModels"
              xmlns:data="clr-namespace:Mobile.Code.Data"
             x:Class="Mobile.Code.Views.ProjectNew">


    <ContentPage.BindingContext>
        <vm:ProjectViewModel></vm:ProjectViewModel>
    </ContentPage.BindingContext>

    <CollectionView Margin="20" HeightRequest="270"  
                    ItemsSource="{x:Static data:CatData.Cats}"
                    ItemTemplate="{StaticResource AnimalTemplate}"
                    SelectionMode="Single"
                    SelectionChangedCommand="{Binding ProjectDetailCommand }" >
        <CollectionView.ItemsLayout>
        <LinearItemsLayout Orientation="Horizontal"  />
        </CollectionView.ItemsLayout>
    </CollectionView>

</ContentPage>